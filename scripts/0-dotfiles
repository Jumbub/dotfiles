#/usr/bin/env sh

set -e

# Warn user about potential changes
echo "The following files will be overidden:"
diff $HOME/repos/dotfiles $HOME -r | grep -e ">\|<"
if ! ./confirm
then
	echo "Aborting"
	exit
fi

