#/usr/bin/env sh

# Update package list
sudo pacman -Sy

install() {
  sudo pacman -S --needed --noconfirm $@
}

install neovim
install redshift
install kitty
install firefox
install git
install zsh
install ripgrep
install alsa-utils
install nvidia
install nvidia-utils
install openssh
