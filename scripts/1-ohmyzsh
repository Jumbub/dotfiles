#/usr/bin/env sh

echo
echo "!!!!! After you install, exit oh-my-zsh to finish running script."
echo

# Install oh-my-zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

# Undo zshrc override
mv ~/.zshrc.pre-oh-my-zsh ~/.zshrc
